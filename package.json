{
  "name": "the-good-corner",
  "version": "1.0.0",
  "description": "_Prerequisites: Docker (24+), Make._",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format:fix": "npx prettier ./**/*.{ts,tsx} --write"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "prettier": "^3.2.5"
  }
}
