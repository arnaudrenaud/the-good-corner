on:
  push:
    branches:
      - main

jobs:
  todo-to-issue:
    name: TODO to Issue
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: alstr/todo-to-issue-action@v4.12.3
